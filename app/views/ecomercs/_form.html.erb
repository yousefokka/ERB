<%= form_with(model: ecomerc, local: true) do |form| %>
  <% if ecomerc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ecomerc.errors.count, "error") %> prohibited this ecomerc from being saved:</h2>

      <ul>
      <% ecomerc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :department_id %>
    <%= form.text_field :department_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
